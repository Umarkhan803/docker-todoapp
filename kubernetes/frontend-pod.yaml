apiVersion: v1
kind: Pod
metadata:
  name: frontend-pod 
  labels:
    app: frontend-pod
spec:
  containers:
    - name: frontend-pod
      image: umarkhan803/todo-frontend
      imagePullPolicy: Never
      ports:
        - containerPort: 80
          name: frontend 
