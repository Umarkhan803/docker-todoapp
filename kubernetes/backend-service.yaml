apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend-service
spec:
  type: NodePort
  ports:
    - name: backend-service
      port: 5000
      targetPort: 5000
      nodePort: 31001
  selector:
    app: backend-pod